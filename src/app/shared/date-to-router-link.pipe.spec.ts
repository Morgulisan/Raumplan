import { DateToRouterLinkPipe } from './date-to-router-link.pipe';

describe('DateToRouterLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToRouterLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
